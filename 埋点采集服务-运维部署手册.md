# 埋点采集服务（Analytics Ingest）运维部署手册

本手册用于指导在字节云/火山引擎环境下，通过 Nginx 配置同源代理，将官网前端的 `/api/analytics/*` 请求转发到后端采集服务（FastAPI）。

## 1. 服务概览
- 框架/语言：Python FastAPI（异步 MySQL）
- 容器监听端口：8080（容器内）
- 健康检查：`GET /healthz` → 200
- 接口：`POST /api/analytics/track`、`POST /api/analytics/track/batch`
- Content-Type：`application/json` 与 `text/plain;charset=UTF-8`（sendBeacon 兼容）
- 返回：成功 204（无 body）；响应头携带 `X-Request-Id`（首条事件 rid）
- 限制：单请求 ≤ 200KB；批量 ≤ 50 条

## 2. 前置条件
- 机器具备 Docker、Nginx（或等价网关）
- 可访问 MySQL：`dbconn.sealosbja.site:43919`（账号由工程给出）
- 官网域名 TLS 证书已就绪（若切换新域名，同步更新证书）

## 3. 构建与发布镜像
```bash
# 在代码仓库根目录执行（按需替换 <REGISTRY>）
docker build -t <REGISTRY>/analytics-ingest:v1 .
docker push <REGISTRY>/analytics-ingest:v1
```

## 4. 启动容器（与 Nginx 同机）
仅本机监听，避免公网直暴露。
```bash
docker run -d --name analytics-ingest --restart unless-stopped \
  -p 127.0.0.1:8080:8080 \
  -e DB_HOST=dbconn.sealosbja.site \
  -e DB_PORT=43919 \
  -e DB_USER=root \
  -e DB_PASSWORD=qrzk4ts4 \
  -e DB_NAME=analytics \
  -e ALLOWED_ORIGINS='["https://<YOUR_DOMAIN>"]' \
  <REGISTRY>/analytics-ingest:v1

# 健康检查（本机）
curl -s http://127.0.0.1:8080/healthz
```

## 5. Nginx 同源代理配置
在官网域名的 `server {}` 中增加：
```nginx
# 上游后端（容器映射到本机 8080）
upstream analytics_ingest {
  server 127.0.0.1:8080;
  keepalive 32;
}

# 外部 /api/analytics/* → 本机容器
location ^~ /api/analytics/ {
  proxy_pass http://analytics_ingest;  # 保留原始路径 /api/analytics/...
  proxy_http_version 1.1;

  # 真实 IP 与可观测
  proxy_set_header Host               $host;
  proxy_set_header X-Real-IP          $remote_addr;
  proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Proto  $scheme;
  proxy_set_header X-Server-IP        $server_addr;

  # 体积/超时（与应用限制保持一致）
  client_max_body_size 200k;
  proxy_connect_timeout 1s;
  proxy_read_timeout    3s;
  proxy_send_timeout    3s;

  # 预检（通常同源无需，但可留作兜底）
  if ($request_method = OPTIONS) { return 204; }
}
```
验证并重载：
```bash
nginx -t && nginx -s reload
```

可选：在 `http {}` 内配置限流，再在上述 `location` 中启用。
```nginx
limit_req_zone $binary_remote_addr zone=analytics:10m rate=5r/s;
# location 内：
# limit_req zone=analytics burst=20 nodelay;
```

## 6. 外部验证（同源）
```bash
# 替换为生产官网域名
curl -i -X POST https://<YOUR_DOMAIN>/api/analytics/track \
  -H 'Content-Type: application/json' \
  -d '{
    "version":"v1",
    "eventName":"page_view",
    "eventId":"c1c3c0c1-1e2f-4b5a-9ac1-0000000000aa",
    "rid":"2f7a6c10-5b0c-4b7e-8b7e-0000000000aa",
    "timestamp":"2025-08-11T09:00:00.000Z"
  }'
# 预期：204；响应头含 X-Request-Id
```

## 7. 换域名如何调整
- DNS：新域名解析到公网入口（CLB/弹性 IP）
- 证书：替换为新域名证书
- Nginx：`server_name new-domain.com;` 改为新域名，其余代理逻辑不变
- 后端：容器变量 `ALLOWED_ORIGINS='["https://new-domain.com"]'` 更新并重启容器

## 8. 回滚与故障排查
- 回滚
  - 保留上一版本镜像；问题时重新运行旧标签
  - Nginx 保留上一次可用配置，`nginx -t` 通过后再 `reload`
- 常见问题
  - 404：`location ^~ /api/analytics/` 未生效或未 reload；或容器未启动
  - 413：请求体 > 200KB；前端需分批；`client_max_body_size` 需 200k
  - 422：校验失败（`eventId/rid/sessionId` 必须 UUID v4；`sessionId` 可不传）
  - 5xx：查看容器日志 `docker logs --tail=200 analytics-ingest`
  - DB 错误 Unknown database `analytics`：确保库名/表已创建，环境变量 `DB_NAME=analytics`

## 9. 快速观测与核对
- 健康检查：`curl -s http://127.0.0.1:8080/healthz`
- 入库核对（示例 SQL）：
```sql
SELECT COUNT(*) FROM analytics_events;
SELECT HEX(event_id) AS event_id, event_name, received_at
FROM analytics_events
ORDER BY received_at DESC
LIMIT 20;
```

## 10. 环境变量一览
- `DB_HOST`、`DB_PORT`、`DB_USER`、`DB_PASSWORD`、`DB_NAME=analytics`
- `ALLOWED_ORIGINS`：JSON 数组字符串，如 `["https://<YOUR_DOMAIN>"]`
- `MAX_BATCH_EVENTS`（默认 50）
- `MAX_REQUEST_BYTES`（默认 204800）

—— 完 ——
