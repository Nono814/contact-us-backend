# 🚀 云服务器部署指南

## 概述
本指南将帮助你将 officialwebbackend 部署到云服务器上，并提供不依赖 Git 的 rsync（密码登录）同步方案。

## 前置要求

### 1. 服务器要求
- 操作系统: Ubuntu 20.04+ 或 CentOS 8+
- 内存: 最少 1GB RAM
- 存储: 最少 10GB 可用空间
- 网络: 公网 IP 地址

### 2. 域名和 SSL 证书（可选）
- 域名（可选，但推荐）
- SSL 证书（可使用 Let's Encrypt 免费证书）

## 部署步骤

### 第一步：服务器环境准备

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装 Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version
npm --version

# 安装 PM2
sudo npm install -g pm2

# 安装 Nginx
sudo apt install nginx -y

# 安装防火墙
sudo ufw enable
sudo ufw allow ssh
sudo ufw allow 80
sudo ufw allow 443
```

### 第二步：代码部署（不使用 Git，rsync + 密码登录）

以下命令已替你填好云服务器信息（IP、端口、密码）。

- 云服务器 IP: 115.190.117.78
- 端口: 22
- 部署目录: /srv/officialwebbackend/
- 密码: Aiedn531
- 用户名: 请将 <user> 替换为你的实际 SSH 用户（如 root、ubuntu 等）

1) 本地安装依赖工具（只需一次）
```bash
sudo apt install -y sshpass rsync
```

2) 在云服务器上准备部署目录（若无）
```bash
SSHPASS='Aiedn531' sshpass -e ssh \
  -p 22 -o StrictHostKeyChecking=no \
  <user>@115.190.117.78 \
  'sudo mkdir -p /srv/officialwebbackend && sudo chown -R <user>:<user> /srv/officialwebbackend'
```

3) 预演（不真正传输，确认将要同步/删除的文件）
```bash
SSHPASS='Aiedn531' sshpass -e rsync -avz --dry-run --delete \
  --exclude ".git" --exclude "node_modules" --exclude "logs" --exclude "*.log" \
  --exclude "config.env" --exclude "config.production.env" --exclude ".vscode" --exclude ".env*" \
  -e "ssh -p 22 -o StrictHostKeyChecking=no" \
  /home/devbox/project/ <user>@115.190.117.78:/srv/officialwebbackend/
```

4) 实际同步（执行传输）
```bash
SSHPASS='Aiedn531' sshpass -e rsync -avz --delete \
  --exclude ".git" --exclude "node_modules" --exclude "logs" --exclude "*.log" \
  --exclude "config.env" --exclude "config.production.env" --exclude ".vscode" --exclude ".env*" \
  -e "ssh -p 22 -o StrictHostKeyChecking=no" \
  /home/devbox/project/ <user>@115.190.117.78:/srv/officialwebbackend/
```

5) 登陆云服务器并部署启动（在服务器上执行）
```bash
cd /srv/officialwebbackend
# 创建并填写生产环境变量（不要从本地同步）
nano config.production.env
# 部署并启动
chmod +x deploy-production.sh
./deploy-production.sh
```

配置示例请参考下方“环境变量配置”。

### 第三步：配置 Nginx

```bash
# 备份默认配置
sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/default.backup

# 复制配置（在项目根目录下）
sudo cp nginx.conf /etc/nginx/sites-available/officialwebbackend

# 启用站点
test -L /etc/nginx/sites-enabled/officialwebbackend || \
  sudo ln -s /etc/nginx/sites-available/officialwebbackend /etc/nginx/sites-enabled/

# 删除默认站点（如果存在）
sudo rm -f /etc/nginx/sites-enabled/default

# 测试并重启 Nginx
sudo nginx -t
sudo systemctl restart nginx
sudo systemctl enable nginx
```

### 第四步：SSL 证书（可选）

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
# 自动续期（可选）
# 0 12 * * * /usr/bin/certbot renew --quiet
```

### 第五步：监控和日志

```bash
# 查看应用状态
pm2 status

# 查看应用日志（PM2）
pm2 logs officialwebbackend

# 监控资源使用
pm2 monit

# 查看 Nginx 日志
sudo tail -f /var/log/nginx/officialwebbackend-access.log
sudo tail -f /var/log/nginx/officialwebbackend-error.log
```

## 环境变量配置

将以下内容写入服务器上的 `config.production.env`：

```env
# 数据库配置
DB_HOST=dbconn.sealosbja.site
DB_PORT=43919
DB_USER=root
DB_PASSWORD=qrzk4ts4
DB_NAME=get in touch

# 服务器配置
PORT=8080
NODE_ENV=production

# 邮件通知配置
SMTP_USER=18264190169@163.com
SMTP_PASS=XUnRzEm8M89jcfQe
ADMIN_EMAILS=18264190169@163.com,zzw814@163.com
```

## 常用管理命令

```bash
# 重启应用
pm2 restart officialwebbackend

# 停止应用
pm2 stop officialwebbackend

# 查看详细信息
pm2 show officialwebbackend

# 查看日志
pm2 logs officialwebbackend --lines 100
```

## 故障排除

```bash
# 应用无法启动 -> 看日志
pm2 logs officialwebbackend

# 检查端口占用
sudo netstat -tlnp | grep :8080

# 检查环境变量（PM2）
pm2 env officialwebbackend

# 测试 Nginx 配置
sudo nginx -t

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/error.log
```

## 安全建议

```bash
# 定期更新系统
sudo apt update && sudo apt upgrade -y

# 配置防火墙
sudo ufw enable
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow ssh
sudo ufw allow 80
sudo ufw allow 443

# 监控工具
sudo apt install -y htop
htop
```

## 说明
- rsync 命令中使用了 `sshpass` 的环境变量方式（`SSHPASS='Aiedn531' sshpass -e ...`）。建议部署后改用 SSH Key 认证。
- 请将命令中的 `<user>` 替换为你的实际 SSH 用户名（如 `root` 或 `ubuntu`）。
- 我们在同步时排除了敏感/无关文件：`.git/`, `node_modules/`, `logs/`, `*.log`, `config*.env`, `.vscode/`, `.env*`。

---

## 第二步：代码部署（详解）
- 安装 `sshpass` 与 `rsync`：
  - `rsync` 用于“增量同步”，只传变更，快速且可排除文件。
  - `sshpass` 允许用密码非交互登录 SSH（临时方案，长期建议改 SSH Key）。
- 准备目录命令含义：
  - `sudo mkdir -p /srv/officialwebbackend`：创建部署目录（已存在不报错）。
  - `sudo chown -R <user>:<user> /srv/officialwebbackend`：将目录所有权授给你的 SSH 用户，便于后续写入。
- 预演同步：
  - `--dry-run` 只显示将会发生的变更，不实际传输。
  - `--exclude` 排除敏感与无关内容。
  - `--delete` 清理服务器上本地已删除的旧文件，避免脏状态。
  - 源路径末尾的 `/` 很重要，表示“同步目录内容而非目录本身”。
- 实际同步：与预演相同，去掉 `--dry-run`。
- 服务器部署：
  - `config.production.env` 仅在服务器创建与维护，切勿从本地同步或提交到 Git。
  - `deploy-production.sh` 会：安装依赖、测试数据库连接、通过 PM2 启动应用并执行健康检查。

## 第三步：Nginx 配置（详解）
- 配置文件位置：
  - 推荐放置在 `/etc/nginx/sites-available/officialwebbackend`，并软链到 `sites-enabled`。
- 关键指令说明：
  - `server_name your-domain.com`：换成你的域名；无域名也可用服务器 IP（去掉 HTTPS 强制跳转）。
  - `listen 80` 与 80→443 跳转：将 HTTP 自动重定向到 HTTPS。
  - `ssl_certificate`/`ssl_certificate_key`：证书与私钥路径；使用 Certbot 后通常在 `/etc/letsencrypt/live/<your-domain>/`。
  - `access_log`/`error_log`：日志路径已命名为 `officialwebbackend-*`，便于区分。
  - `location /api/`、`/health`、`/admin`、`/`：反代到 `http://localhost:8080`，与应用 `PORT=8080` 对应。
  - 代理头：`X-Forwarded-*` 与 `Host` 等保证源 IP、协议等透传；应用内已 `app.set('trust proxy', true)`。
- 常用命令：
  - 测试配置：`sudo nginx -t`
  - 重新加载：`sudo systemctl reload nginx` 或 `sudo systemctl restart nginx`
  - 查看日志：`sudo tail -f /var/log/nginx/officialwebbackend-error.log`

## PM2 配置（详解）
- 配置文件 `ecosystem.config.js` 关键项：
  - `name: 'officialwebbackend'`：进程名。
  - `script: 'server.js'`：启动入口。
  - `instances: 'max'` + `exec_mode: 'cluster'`：使用所有 CPU 核心并行处理请求。
  - `env_file: './config.production.env'`：从文件加载环境变量（可改为绝对路径，如 `/etc/officialwebbackend/config.production.env`）。
  - `max_memory_restart: '1G'`：内存超过 1G 自动重启。
  - `watch: false`：生产环境不建议监听文件变更。
  - 日志：`logs/` 目录下 `out.log/err.log/combined.log`。
- 常用命令：
  - 启动：`pm2 start ecosystem.config.js`
  - 重启：`pm2 restart officialwebbackend`
  - 停止/删除：`pm2 stop officialwebbackend` / `pm2 delete officialwebbackend`
  - 查看：`pm2 status`、`pm2 show officialwebbackend`
  - 日志：`pm2 logs officialwebbackend --lines 100`
  - 自启：`pm2 startup`（按提示执行 root 命令）+ `pm2 save`
- 日志轮转（可选）：
  - `pm2 install pm2-logrotate`
  - `pm2 set pm2-logrotate:max_size 10M`、`pm2 set pm2-logrotate:retain 30` 等。

## 环境变量（详解）
- 变量含义：
  - `DB_*`：数据库连接（主机、端口、用户名、密码、数据库名）。
  - `PORT`：应用监听端口（与 Nginx 反代目标一致）。
  - `NODE_ENV`：设为 `production` 以启用生产模式优化。
  - `SMTP_*`、`ADMIN_EMAILS`：邮件通知配置。
- 建议：
  - 仅在服务器保存，权限设置为 `chmod 600 config.production.env`。
  - 切勿提交到 Git，`.gitignore` 已忽略。
  - 若迁移到 `/etc/officialwebbackend/config.production.env`，需同步修改 `ecosystem.config.js` 的 `env_file` 为绝对路径。

## 使用 Git 同步代码（可选）
- 初次部署：
  ```bash
  sudo mkdir -p /srv/officialwebbackend && sudo chown -R <user>:<user> /srv/officialwebbackend
  cd /srv/officialwebbackend
  # SSH 方式（推荐）
  git clone git@github.com:<your-username>/<your-repo>.git .
  # 或 HTTPS（私有仓库需 PAT）
  # git clone https://github.com/<your-username>/<your-repo>.git .
  npm ci --only=production || npm install --production
  pm2 start ecosystem.config.js && pm2 save && pm2 startup
  ```
- 后续更新：
  ```bash
  cd /srv/officialwebbackend
  git pull --rebase
  npm ci --only=production || npm install --production
  pm2 reload officialwebbackend
  ```
- SSH Key 配置：
  ```bash
  ssh-keygen -t ed25519 -C "your@email"
  cat ~/.ssh/id_ed25519.pub  # 添加到 GitHub SSH Keys
  ssh -T git@github.com      # 测试
  ```
- 注意：
  - 保持 `.gitignore` 忽略 `config*.env`、日志等敏感/临时文件。
  - 若 Git 不可用，继续使用本指南前述 `rsync` 方案。